import SimpleMarkdown from "./simple-markdown/simple-markdown"
import type * as SimpleMarkdownType from "./simple-markdown/simple-markdown"

const targetAST = [
    {
        type: "heading",
    },
    {
        type: "list",
        bullet: "-",
    },
]

const getInnerText = (ast: any) => {}
